{
  "buildCommand": "cd client && npm install --registry https://registry.npmjs.org && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'skip root install'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
