{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm config set registry https://registry.npmjs.org && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
